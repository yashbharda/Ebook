services:
  - type: web
    name: ebook-management
    buildCommand: javac -d target src/**/*.java
    startCommand: java -cp target ServletLogin
    envVars:
      - key: JAVA_VERSION
        value: 17
