services:
  - type: web
    name: ebook-management
    buildCommand: javac -d target src/**/*.java
    startCommand: java -cp target YourMainClass
    envVars:
      - key: JAVA_VERSION
        value: 17
